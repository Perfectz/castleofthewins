Read `AGENTS.md`, `docs/ai/PROJECT_BRIEF.md`, `ARCHITECTURE.md`, `SYSTEM_MAP.md`, and `package.json`.

Then produce a compact project orientation with:

1. The runtime entrypoints and generated files.
2. The module ownership map.
3. The commands used for build, test, screenshots, and playtesting.
4. The main risk areas for future edits.
5. Any documentation drift you notice between source layout and docs.

Do not start coding unless the user asks for changes.
